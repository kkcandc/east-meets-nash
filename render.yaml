services:
  - type: web
    name: east-meets-nash-web
    runtime: node
    plan: starter
    region: oregon
    rootDir: web
    buildCommand: npm ci && npm run build
    startCommand: npx next start -p $PORT
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_SITE_URL
        value: https://eastmeetsnash.com
      - key: GOOGLE_PLACES_API_KEY
        sync: false
